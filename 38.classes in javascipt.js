class jeep {
    constructor(){
        console.log("Constructor is created!")
    }
    start() {
        console.log("The jeep will have the feature to get start.")
    }
    start(){
        console.log("The jeep will have the feature to  stop.")
    }
}
// now suppose i am creating and jeeb i want the feature of the jeep i already declared
// Japi jeep name
let japi= new jeep()