let a =15 ;
let b= 18;
// if one condition is false answer will be false
console.log("a >b && a <12",a >b && a <12); // false

// if one condition is true answer will be true
console.log("a<b || a <12",a<b || a<12); // true


// not operator is used to revert the result 
console.log("a<b || a <12", !(a<b)); // true but print false
